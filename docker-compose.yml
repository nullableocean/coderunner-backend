version: '3.8'
services:
  nginx:
    image: nginx:latest
    depends_on:
      - app
    ports:
      - "${PORT}:80"
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf
    
  app:
    build: .